import Layout from "@/app/layout";
import RootLayout from "@/app/layout";

export default function Home() {
    return (
        <Layout>
            <main>
                <h1> Hello world!"</h1>
            </main>
        </Layout>
    );
}
